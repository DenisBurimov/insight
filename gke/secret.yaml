# Template only — do NOT commit real values.
# Fill in each field and apply with:  kubectl apply -f gke/secret.yaml
# Or create imperatively:
#   kubectl create secret generic insight-secrets -n insight \
#     --from-literal=SECRET_KEY=... \
#     --from-literal=SQLALCHEMY_DATABASE_URI=... \
#     ...
apiVersion: v1
kind: Secret
metadata:
  name: insight-secrets
  namespace: insight
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_ME"
  SQLALCHEMY_DATABASE_URI: "postgresql://user:password@host:5432/insight"
  OPENAI_API_KEY: "sk-REPLACE_ME"
  SCHEDULER_ACCESS_TOKEN: "REPLACE_ME"
  ADMIN_USERNAME: "admin"
  ADMIN_EMAIL: "admin@example.com"
  ADMIN_PASSWORD: "REPLACE_ME"
